import { getConnection, Equal } from 'typeorm'
import Boom from '@hapi/boom'
import { Status, PaymentMethod, PaymentRequest } from '../../entities/paymentRequest'
import of from 'await-of'
import logger from '../../lib/logger'

export class BaseController {

}
